name: Deploy hermees to Cloudflare

on:
  push:
    branches: [main]
    paths:
      - 'hermees/**'
      - '.github/workflows/deploy-hermees.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: cd hermees && npm install

      - name: Build
        run: cd hermees && npm run build

      - name: Lint
        run: cd hermees && npm run lint || true

      - name: Type check
        run: cd hermees && npm run check || true

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist --project-name=hermees-diary
        env:
          WRANGLER_CONFIG_DIR: hermees

      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '✅ Hermees deployed to Cloudflare Pages\n\nPreview: https://hermees.yourdomain.com'
            })
