[
  {
    "app": "homely",
    "path": "automation/client",
    "purpose": "Outbound WebSocket adapter plus core+plan domain layers driving homely over frozen ws-protocol.md v1. B1 handshake+empty-home store (reviewed, eae72ba fixes). B2 full typed model, snapshot undo/redo, deterministic serializer, ajv fixtures (adversarial review REQUEST-CHANGES → all 10 findings fixed 0294efd; harness equivalence 22/22+21/21). B3 headless PlanEngine state machine ported from SH3D PlanController: wall chaining with free-end snapping + 15°/ladder magnetism, dbl-click loop-close w/ exact join + clone-deviation auto-room, escape-commits-rest, click/shift/drag selection semantics, one compound undo per gesture; handler grows to 14 commands delegating select_tool/click/drag/key/set_magnetism to a shared PlanEngine; canvas renderer (drawPlan) renders store snapshots + live preview. 84/84 vitest incl equivalence-style script mirroring scenarios/walls/create_room.yaml (wall graph modulo ids).",
    "title": "Homely Automation WS Client + Core/Plan Domain (B1-B3)"
  },
  {
    "app": "homely",
    "path": "automation/protocol",
    "purpose": "Single automation contract implemented identically by sh3d-driver (Java), homely clone client (TS), and harness MockAdapter (Python); enables parallel track development and differential testing. FROZEN for Wave 1 - changes only via integrator approval.",
    "title": "Homely Automation WebSocket Protocol v1 (Frozen for Wave 1)"
  },
  {
    "app": "homely",
    "path": "core/export-parity-defaults",
    "purpose": "B8 state-export + defaults parity with the real SH3D 7.5 driver golden (equivalence/scenarios/slice/goldens/create_room.expected-state.json), per frozen contract docs/behaviours/sh3d-camera-and-export.md §2-§3: wall creation defaults (thickness 7cm, patternId 'hatchUp'), HomeEnvironment wallsAlpha default 0 (transparency semantics), compass location from OS timezone via ported Compass.java table (core/compass-timezones.ts), stable camera ids in export, wire rounding rules (round3 HALF_EVEN for lengths/fovDeg/compass radians; angles wrapped [-180,180) then round3).",
    "title": "homely core export-parity-defaults (B8)"
  },
  {
    "app": "homely",
    "path": "core/top-camera-follower",
    "purpose": "Ports SH3D HomeController3D$TopCameraState (HomeController3D.java:823-1330): the top camera re-places itself on every home CONTENT change, orbiting the new whole-home bounds center at its previous distance (clamped to [halfDiag*1.05, max(5*minDist,5000)]), keeping yaw/pitch/fov. Wired into HomeStore apply/undo/redo so automation, plan tool and GUI share identical placement. Reproduces the numeric verification table in docs/behaviours/sh3d-camera-and-export.md §1 exactly. (B8 update: runtime NEW_WALL_THICKNESS_CM is now 7cm and wall chains carry patternId 'hatchUp' — driver-default aligned; no follower logic changed.)",
    "title": "homely core top-camera-follower (B7)"
  },
  {
    "app": "homely",
    "path": "documents/knowledge/homely-architecture-map",
    "purpose": "Prevents redundant source exploration; encodes verified SH3D automation paths, defaults, file-format facts, and the clone/harness design decisions.",
    "title": "Homely Architecture Map (SH3D research record)"
  },
  {
    "app": "homely",
    "path": "plan/engine",
    "purpose": "Headless SH3D PlanController port for homely: per-click wall commits inside ONE compound undo session (B9), SH3D-exact chaining/joins/magnetism, selection interactions.",
    "title": "Homely Plan Engine — headless PlanController port (B3, revised B9)"
  },
  {
    "app": "house_designer",
    "path": "equivalence/eq/comparators/diff",
    "purpose": "Tolerance-aware deep diff between two NormalizedHomeState documents (driver golden vs homely export). Produces Failure records with path/kind/expected/actual/delta/objectId; powers compare_states (ledger id-matching) and artifact-level comparisons in run.py. C8 added null-normalization: adapters legitimately serialize 'no value' differently (SH3D driver emits explicit JSON null, homely omits the key).",
    "title": "eq.comparators.diff — tolerant deep-diff with null==absent normalization"
  },
  {
    "app": "house_designer",
    "path": "equivalence/eq/comparators/visual",
    "purpose": "C5 visual-diff comparator: pixel comparison of two PNG screenshots producing a structured VisualResult (match bool + diff metrics), proving rendered output equivalence on top of the state deep-diff. Threshold breach flips the scenario verdict.",
    "title": "Eq.Comparators.Visual — C5 PNG pixel-diff with score, threshold verdict, heatmap"
  },
  {
    "app": "house_designer",
    "path": "homely/src/automation/capture",
    "purpose": "Deterministic offscreen screenshot service backing the frozen ws-protocol screenshot command (plan + 3d views).",
    "title": "homely capture — deterministic offscreen screenshot (plan/3d) for the screenshot command"
  },
  {
    "app": "house_designer",
    "path": "homely/src/view3d",
    "purpose": "Three.js 3D view of the normalized home: SH3D-exact camera presets (top/observer), deterministic scene-graph build from core store state, live re-render on store mutation, and CameraDirector backing the set_camera/camera_preset automation commands.",
    "title": "homely view3d — Three.js scene with SH3D-exact cameras and live store sync"
  },
  {
    "app": "equivalence",
    "path": "eq/adapters/live",
    "purpose": "Bridge the equivalence harness to real automation endpoints: Sh3dAdapter connects out to the Java driver's FramedServer over framed TCP; HomelyAdapter adopts the inbound homely WS session on an AutomationServer. Both expose the Adapter ABC so the Orchestrator runs them exactly like MockAdapter.",
    "title": "Live adapters: Sh3dAdapter TCP client + HomelyAdapter session wrapper"
  },
  {
    "app": "house_designer",
    "path": "hermes-engineering/setup",
    "purpose": "Provides the complete file structure and coordination protocol for a multi-agent engineering team coordinated via Steward ACS. Includes universal rules (AGENTS.md), coordination protocol (AGENTS_STEWARD.md), claim board (PLAN.md), orchestrator agent (engineering-manager.md), worker template (worker.md), local CI gate (verify-all.sh), and Steward MCP configuration (opencode.json).",
    "title": "Hermes Engineering Team Setup — Multi-agent coordination with Steward"
  }
]
